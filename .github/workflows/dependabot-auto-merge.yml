name: Dependabot Auto-Merge

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: write
  pull-requests: write

jobs:
  workflows:
    uses: 21stdigital/workflows/.github/workflows/dependabot-auto-merge.yml@main
